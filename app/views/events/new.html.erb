<div class="container mt-5 mb-5">
  <div class="row">
    <div class="col"></div>

    <div class="col-6">
      <div class="event-form">
        <%= simple_form_for([@trip, @event]) do |f| %>
          <%= f.input :name %>
          <%= f.input :address %>
          <%= f.submit %>
        <% end %>
      </div>
    </div>

    <div class="col"></div>
  </div>
</div>
