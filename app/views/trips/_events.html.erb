<div class="events" data-toggle-target="events" data-action="change->maps#updateMap">
  <% events.each_with_index do |event, index| %>
    <%= render "event", event: event, index: index %>
  <% end %>
</div>
